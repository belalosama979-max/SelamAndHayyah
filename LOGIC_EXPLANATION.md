# شرح آلية ومنطق نظام "سلم وحية - طريق الأقصى"

هذا المستند يشرح بشكل تفصيلي المنطق (Logic) الخاص بجميع أجزاء النظام ليكون مرجعاً واضحاً لأي مطور أو مالك جديد للمشروع.

---

## 1. إدارة الغرف (النسخ)
النظام يدعم إنشاء عدد غير محدود من **الغرف (Rooms)** أو "النسخ".
كل غرفة تعمل كبيئة معزولة تماماً لها:
- قائمة طلاب (Players) خاصة بها.
- سجل حركات (Logs) خاص بها.
- إعدادات خاصة إن لزم (رغم أن الهدف والسعة حالياً موحد في الإعدادات العامة).

**المنطق:**
- عند إضافة طالب، يتم ربطه بمعرف الغرفة `roomId`.
- جميع العمليات (إضافة نقاط، تراجع، عرض السجل) تتم بتصفية البيانات بناءً على `roomId`.
- يمكن "أرشفة" الغرف لإخفائها من الواجهة النشطة دون حذف بياناتها.

---

## 2. نظام النقاط والتقدم (Points & Progress)
كل طالب يمتلك ثلاث خصائص أساسية مرتبطة بالنقاط:
1. `points`: النقاط الحالية الفعالة التي تحدد موقعه على الخريطة (لا تنقص عند الشراء من المتجر).
2. `totalCollectedPoints`: إجمالي النقاط التي جمعها الطالب طوال فترة اللعب (رقم لا ينقص أبدًا ويستخدم للإحصائيات).
3. `rewardPoints`: النقاط المتاحة للشراء من المتجر (تساوي `totalCollectedPoints - totalSpent`).

**حساب التقدم (Progress Percentage):**
- نسبة التقدم = `(points / targetPoints) * 100`
- `targetPoints` هو الهدف النهائي للعبة (افتراضياً 8500 نقطة ويمكن تغييره من الإعدادات).

---

## 3. الخريطة وحساب المواقع (Board & Positions)
الخريطة تتكون من عدد من الخانات (افتراضياً 100 خانة `boardSize`).

**حساب الموقع الحالي:**
- قيمة الخطوة الواحدة (الخانة) = `targetPoints / boardSize` (مثلاً 8500 / 100 = 85 نقطة لكل خانة).
- الموقع الحالي = `1 + Math.floor(points / قيمة الخطوة الواحدة)`.
- أقصى موقع ممكن هو خانة النهاية (100).

---

## 4. السلالم والأفاعي (Ladders & Snakes - Board Events)
تمتلك الخريطة أحداثاً مبنية مسبقاً توجد في `seedData.js` ضمن `DEFAULT_BOARD_EVENTS`.
- **السلم (Ladder):** له نقطة بداية `startPosition` ونقطة نهاية أعلى `endPosition`. إذا وصل الطالب لنقطة البداية، يصعد تلقائياً للنهاية.
- **الأفعى (Snake):** لها نقطة بداية `startPosition` ونقطة نهاية أدنى `endPosition`. إذا وصل الطالب لنقطة البداية، ينزلق تلقائياً للنهاية.

**متى يتم تفعيلها؟**
عند تطبيق بطاقة على الطالب، يتم حساب موقعه الجديد. إذا تطابق موقعه مع `startPosition` لحدث معين، يتم تحريكه فوراً إلى `endPosition` ويتم تعديل نقاطه `points` لتتناسب مع موقعه الجديد.

---

## 5. نظام البطاقات (Cards)
البطاقات هي الوسيلة الوحيدة لإضافة أو خصم النقاط من الطلاب.
- لكل بطاقة قيمة ثابتة `value` أو قيمة متغيرة (تُدخل يدوياً عند التطبيق).
- البطاقات مقسمة لتصنيفات (تجويد، حفظ، متابعة تربوية).
- يتم تسجيل كل تطبيق لبطاقة في جدول السجلات `LOGS`، مما يتيح ميزة **التراجع (Undo)** في حال الخطأ.

---

## 6. المتجر والجوائز (Shop & Prizes)
يعتمد المتجر على نقاط المكافآت `rewardPoints` فقط، ولا يؤثر على تقدم الطالب في الخريطة `points`.

**آلية الشراء:**
1. يختار الطالب الجائزة.
2. إذا كان يمتلك الرصيد الكافي، يتم إنشاء **طلب جائزة (Prize Request)** وتكون حالته `pending` (قيد الانتظار).
3. تُخصم النقاط مؤقتاً من رصيد الطالب.
4. يظهر الطلب في لوحة تحكم الإدارة (القسم الخاص بالمتجر).
5. إذا وافق المشرف (`approved`)، تُعتمد العملية ويقل مخزون الجائزة.
6. إذا رفض المشرف (`rejected`)، تُرَد النقاط المخصومة إلى الطالب.

**ملاحظة:** يمكن تفعيل عروض "Flash Sale" لخصم أسعار الجوائز مؤقتاً في أوقات معينة.

---

## 7. بوابة ولي الأمر (Parent Portal)
لكل طالب رمز دخول فريد `parentCode`.
يمكن لولي الأمر استخدام هذا الرمز للدخول ورؤية:
- موقع ابنه على الخريطة ومقدار إنجازه.
- سجل البطاقات التي حصل عليها (الإيجابية والسلبية).
- ترتيب ابنه في الغرفة.

---

## 8. الألغاز (Quizzes)
نظام الألغاز يسمح للمشرف بإنشاء أسئلة تفاعلية وتحديد وقت لإتاحتها.
- يمكن للطلاب الدخول وحل اللغز للحصول على مكافأة إضافية يتم تحويلها لنقاط.

---

## 9. قاعدة البيانات المحلية والمزامنة (Database & Sync)
يعتمد النظام بشكل أساسي على `LocalStorage` ليكون فائق السرعة ويعمل بدون إنترنت (Offline First).
- يتم حفظ البيانات محلياً.
- إذا تم تزويد النظام بإعدادات Firebase صحيحة، سيقوم بمزامنة التغييرات المحلية مع السحابة في الخلفية.
- يضمن هذا المعمار إمكانية استخدام التطبيق في المساجد أو المدارس التي لا يتوفر فيها اتصال إنترنت مستقر، مع رفع البيانات عند توفر الاتصال.
